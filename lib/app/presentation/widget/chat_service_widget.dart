import 'package:flutter/material.dart';

class ChatServiceWidget extends StatelessWidget {
  const ChatServiceWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}